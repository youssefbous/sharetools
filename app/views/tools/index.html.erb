<h2>Tools list</h2>

<div class="container">
  <class class="row">
    <% @tools.each do |tool| %>
      <div class="tool">
        <h3><%= link_to tool.title %></h3>

        <p><%= tool.price_per_day %> €/per day</p>
        <p><%= tool.caracteristics %></p>
        <p><%= tool.description %></p>
      </div>
    <% end %>
  </class>
</div>
