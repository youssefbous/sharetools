<div class="container">
  <div class="text-center">
    <h1> <%= @tool.title %> </h1>
    <p> <%= @tool.caracteristics %> </p>
    <p> <%= @tool.description %> </p>
    <p> <%= @tool.price_per_day %> </p>
    <%= link_to 'Edit', edit_tool_path %>
    <%= link_to 'Back', tools_path %>
  </div>
</div>
